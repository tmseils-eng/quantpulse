{
  "name": "quantpulse-client",
  "version": "1.0.0",
  "description": "QuantPulse React frontend — dashboard, stock detail, and paper-trading portfolio views",
  "type": "module",
  "scripts": {
    "dev": "node build.js --serve",
    "build": "node build.js"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
